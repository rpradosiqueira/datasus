% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{taxa_mortalidade}
\alias{taxa_mortalidade}
\title{Calculate grouped mortality rates}
\usage{
taxa_mortalidade(
  dados,
  obitos,
  populacao,
  grupo = NULL,
  multiplicador = 1e+05,
  confianca = NULL,
  na_rm = FALSE,
  zero_denominador = c("na", "erro")
)
}
\arguments{
\item{dados}{A non-empty data frame.}

\item{obitos}{Name of the deaths column.}

\item{populacao}{Name of the population denominator column.}

\item{grupo}{Optional character vector naming grouping columns.}

\item{multiplicador}{Positive rate scale, default 100,000.}

\item{confianca}{Optional confidence level. Exact intervals are available
for rates, proportions and case fatality, but not for ratios.}

\item{na_rm}{Whether rows with a missing numerator or denominator should be
removed pairwise within each group. A group containing no complete row
remains missing.}

\item{zero_denominador}{How to handle zero denominators: `"na"` returns
`NA`, while `"erro"` stops.}
}
\value{
A data frame with aggregated counts and `taxa_mortalidade`.
}
\description{
Calculate grouped mortality rates
}
\examples{
dados <- data.frame(ano = c(2024, 2024), obitos = c(3, 7),
                    populacao = c(5000, 5000))
taxa_mortalidade(dados, "obitos", "populacao", grupo = "ano")
}
