% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opendatasus.R
\name{opendatasus_ler}
\alias{opendatasus_ler}
\title{Download and read tabular OpenDataSUS data}
\usage{
opendatasus_ler(
  conjunto,
  recurso = NULL,
  ano = "last",
  formato = NULL,
  destino = NULL,
  cache = TRUE,
  atualizar = FALSE,
  n_max = Inf,
  colunas = NULL,
  ...
)
}
\arguments{
\item{conjunto}{Dataset slug returned by [opendatasus_catalogo()].}

\item{recurso}{Optional exact resource name or stable resource identifier.}

\item{ano}{Resource year, `"last"` for the latest dated resource, or
`NULL`. Ignored when `recurso` is supplied.}

\item{formato}{Resource format. When both `recurso` and `formato` are
`NULL`, CSV is preferred.}

\item{destino}{Optional destination file or existing directory. `NULL`
uses the package cache, or a temporary file when `cache = FALSE`.}

\item{cache}{Whether to cache portal metadata for the duration controlled by
option `datasus.cache_ttl`.}

\item{atualizar}{Whether to ignore cached metadata and query the portal
again.}

\item{n_max}{Maximum number of CSV rows to read. For JSON data, the limit is
applied after parsing when the result is a data frame.}

\item{colunas}{Optional character vector selecting columns while CSV files
are parsed. JSON columns are selected after parsing.}

\item{...}{Additional arguments passed to [readr::read_delim()] or
[jsonlite::fromJSON()].}
}
\value{
The parsed resource. Attribute `"datasus_proveniencia"` records
  the selected resource, download time, local file and checksum.
}
\description{
Selects and downloads one CSV or JSON resource, then reads it into R.
CSV delimiters are detected automatically. For very large datasets, use
`n_max` while exploring the columns, or call [opendatasus_baixar()] and
use a streaming database engine.
}
\examples{
\donttest{
old_options <- options(
  datasus.timeout = 5,
  datasus.download_timeout = 15,
  datasus.max_tries = 1
)
try({
dengue <- opendatasus_ler(
  "arboviroses-dengue",
  ano = 2025,
  formato = "CSV",
  n_max = 1000
)
datasus_proveniencia(dengue)
})
options(old_options)
}
}
