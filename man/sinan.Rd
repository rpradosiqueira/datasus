% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health-services.R
\name{sinan}
\alias{sinan}
\title{Query notifiable diseases and conditions from SINAN}
\usage{
sinan(
  agravo = "dengue",
  uf = NULL,
  linha = NULL,
  coluna = NULL,
  conteudo = 1,
  periodo = "last",
  filtros = list()
)
}
\arguments{
\item{agravo}{Notifiable disease or condition listed by
`datasus_catalogo("sinan")`.}

\item{uf}{Two-letter state abbreviation. `NULL` uses the Brazil form.}

\item{linha}{Row dimension. `NULL` selects the form's first dimension.}

\item{coluna}{Column dimension. `NULL` selects `"Não ativa"`.}

\item{conteudo}{Measure label, raw TABNET value, or one-based index.}

\item{periodo}{`"last"` for the latest available period, an exact period
label or raw value, a four-digit year, or a vector of these values.}

\item{filtros}{Named list of filters. Names and choices are available from
[datasus_opcoes()].}
}
\value{
A data frame containing the TABNET result.
}
\description{
Supports the disease-specific TABNET datasets listed by
`datasus_catalogo("sinan")`. Because each disease has its own form, inspect
dimensions, periods and filters with `datasus_opcoes("sinan", agravo)`.
Febre amarela is not included because its official link points to a
separate open-data platform rather than a TABNET form.
}
\examples{
\donttest{
old_options <- options(
  datasus.timeout = 5,
  datasus.download_timeout = 15,
  datasus.max_tries = 1
)
try({
sinan("dengue", uf = "MS")
sinan(
  "violencia_interpessoal_autoprovocada",
  uf = "MS",
  periodo = 2024
)
})
options(old_options)
}
}
