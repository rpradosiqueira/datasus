% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{taxa_incidencia}
\alias{taxa_incidencia}
\title{Calculate grouped incidence rates}
\usage{
taxa_incidencia(
  dados,
  casos,
  populacao,
  grupo = NULL,
  multiplicador = 1e+05,
  confianca = NULL,
  na_rm = FALSE,
  zero_denominador = c("na", "erro")
)
}
\arguments{
\item{dados}{A non-empty data frame.}

\item{casos}{Name of the incident cases column.}

\item{populacao}{Name of the population or person-time denominator column.}

\item{grupo}{Optional character vector naming grouping columns.}

\item{multiplicador}{Positive rate scale, default 100,000.}

\item{confianca}{Optional confidence level. Exact intervals are available
for rates, proportions and case fatality, but not for ratios.}

\item{na_rm}{Whether rows with a missing numerator or denominator should be
removed pairwise within each group. A group containing no complete row
remains missing.}

\item{zero_denominador}{How to handle zero denominators: `"na"` returns
`NA`, while `"erro"` stops.}
}
\value{
A data frame with aggregated counts and `taxa_incidencia`.
}
\description{
Calculate grouped incidence rates
}
\examples{
dados <- data.frame(ano = c(2024, 2024), casos = c(10, 20),
                    populacao = c(50000, 50000))
taxa_incidencia(dados, "casos", "populacao", grupo = "ano")
}
