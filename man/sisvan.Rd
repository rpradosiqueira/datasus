% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health-services.R
\name{sisvan}
\alias{sisvan}
\title{Query legacy nutritional surveillance data from SISVAN}
\usage{
sisvan(
  conjunto = "atencao_basica",
  uf = NULL,
  linha = NULL,
  coluna = NULL,
  conteudo = 1,
  periodo = "last",
  filtros = list()
)
}
\arguments{
\item{conjunto}{Dataset name listed by `datasus_catalogo("sih")`.}

\item{uf}{Two-letter state abbreviation. `NULL` uses the Brazil form.}

\item{linha}{Row dimension. `NULL` selects the form's first dimension.}

\item{coluna}{Column dimension. `NULL` selects `"Não ativa"`.}

\item{conteudo}{Measure label, raw TABNET value, or one-based index.}

\item{periodo}{`"last"` for the latest competence, an exact period label or
raw value, a four-digit year (all available competences in that year), or
a vector of these values.}

\item{filtros}{Named list of filters. Names and choices are available from
[datasus_opcoes()].}
}
\value{
A data frame containing the TABNET result.
}
\description{
Provides the historical TABNET series for Primary Care users and Bolsa
Familia beneficiaries. These are legacy series and should not be
interpreted as the current SISVAN coverage.
}
\examples{
\donttest{
old_options <- options(
  datasus.timeout = 5,
  datasus.download_timeout = 15,
  datasus.max_tries = 1
)
try({
sisvan(uf = "MS")
sisvan(conjunto = "bolsa_familia", uf = "MS")
})
options(old_options)
}
}
