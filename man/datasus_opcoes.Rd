% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health-services.R
\name{datasus_opcoes}
\alias{datasus_opcoes}
\title{Inspect the options of a DATASUS query}
\usage{
datasus_opcoes(sistema, conjunto = NULL, uf = NULL, abrangencia = NULL)
}
\arguments{
\item{sistema}{System name accepted by [datasus_catalogo()].}

\item{conjunto}{Dataset name listed by [datasus_catalogo()]. When omitted,
uses the default dataset for the system.}

\item{uf}{State abbreviation, two-digit IBGE code or state name. When
`NULL`, uses the Brazil form.}

\item{abrangencia}{Optional geographic detail for SIM and SINASC:
`"municipio"` for the nationwide municipal form or `"uf"` for the
region/state form. Other systems retain their existing behavior.}
}
\value{
An object of class `datasus_opcoes`. Its components are data frames
  named `linha`, `coluna`, `conteudo` and `periodo`, plus a named list
  `filtros`.
}
\description{
Reads the current TABNET form and returns its row, column, measure,
period and filter choices. Filter names in the returned `filtros` list
are the names accepted by the query functions.
}
\examples{
\dontrun{
op <- datasus_opcoes("sih", uf = "MS")
op$conteudo
op$filtros$carater_atendimento
}
}
