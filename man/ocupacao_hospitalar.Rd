% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contemporary-data.R
\name{ocupacao_hospitalar}
\alias{ocupacao_hospitalar}
\title{Read COVID-19 hospital occupancy records}
\usage{
ocupacao_hospitalar(
  ano = "last",
  formato = "CSV",
  destino = NULL,
  cache = TRUE,
  atualizar = FALSE,
  n_max = Inf,
  colunas = NULL,
  normalizar = FALSE,
  ...
)
}
\arguments{
\item{ano}{Resource year or `"last"` for the latest year currently
published.}

\item{formato}{File format, `"CSV"` or `"JSON"`.}

\item{destino}{Optional destination file or existing directory.}

\item{cache}{Whether to reuse the local OpenDataSUS cache.}

\item{atualizar}{Whether to force a fresh metadata query and download.}

\item{n_max}{Maximum number of rows to read.}

\item{colunas}{Optional character vector selecting columns during CSV
parsing.}

\item{normalizar}{Whether to apply the curated contemporary schema with
[datasus_padronizar()].}

\item{...}{Additional arguments passed to the underlying reader.}
}
\value{
The parsed OpenDataSUS resource with provenance metadata.
}
\description{
Downloads annual records from the e-SUS Notifica Hospital Admissions
module. The published files cover SUS clinical and intensive-care beds
allocated to suspected or confirmed COVID-19 cases. Fields added in 2022
are not populated in earlier records.
}
\examples{
\donttest{
old_options <- options(
  datasus.timeout = 5,
  datasus.download_timeout = 15,
  datasus.max_tries = 1
)
try({
leitos <- ocupacao_hospitalar(ano = 2022, n_max = 1000)
})
options(old_options)
}
}
\references{
OpenDataSUS. Registro de Ocupação Hospitalar COVID-19.
  \url{https://dadosabertos.saude.gov.br/dataset/registro-de-ocupacao-hospitalar-covid-19}
}
