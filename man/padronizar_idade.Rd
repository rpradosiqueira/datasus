% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{padronizar_idade}
\alias{padronizar_idade}
\title{Directly standardize rates by age}
\usage{
padronizar_idade(
  eventos,
  populacao,
  idade,
  populacao_padrao,
  grupo = NULL,
  multiplicador = 1e+05,
  confianca = NULL,
  na_rm = FALSE
)
}
\arguments{
\item{eventos}{Non-negative event counts by age stratum.}

\item{populacao}{Positive population or person-time by age stratum.}

\item{idade}{Age-stratum labels.}

\item{populacao_padrao}{Standard population weights. Supply either one
value per input row or a named vector whose names match `idade`.}

\item{grupo}{Optional analysis group, such as year or municipality. Each age
stratum must occur at most once within a group.}

\item{multiplicador}{Scale of the standardized rate, default 100,000.}

\item{confianca}{Optional confidence level. `NULL` omits confidence limits.}

\item{na_rm}{Whether incomplete age strata should be removed and the
remaining standard weights renormalized.}
}
\value{
A data frame with standardized rate, standard error, confidence
  limits, number of strata and proportion of standard weight represented.
}
\description{
Calculates a weighted average of age-specific rates using a supplied
standard population. Independent Poisson counts are assumed for the
standard error. Optional confidence limits use a normal approximation.
}
\examples{
padronizar_idade(
  eventos = c(10, 40),
  populacao = c(1000, 1000),
  idade = c("0-49", "50+"),
  populacao_padrao = c("0-49" = 800, "50+" = 200)
)
}
\references{
Ahmad OB et al. (2001). Age standardization of rates: a new
  WHO standard. World Health Organization.
}
