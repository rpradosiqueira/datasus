% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{letalidade}
\alias{letalidade}
\title{Calculate grouped case fatality}
\usage{
letalidade(
  dados,
  obitos,
  casos,
  grupo = NULL,
  multiplicador = 100,
  confianca = NULL,
  na_rm = FALSE,
  zero_denominador = c("na", "erro")
)
}
\arguments{
\item{dados}{A non-empty data frame.}

\item{obitos}{Name of the deaths among cases column.}

\item{casos}{Name of the cases column.}

\item{grupo}{Optional character vector naming grouping columns.}

\item{multiplicador}{Positive scale, default 100 for a percentage.}

\item{confianca}{Optional confidence level. Exact intervals are available
for rates, proportions and case fatality, but not for ratios.}

\item{na_rm}{Whether rows with a missing numerator or denominator should be
removed pairwise within each group. A group containing no complete row
remains missing.}

\item{zero_denominador}{How to handle zero denominators: `"na"` returns
`NA`, while `"erro"` stops.}
}
\value{
A data frame with aggregated counts and `letalidade`.
}
\description{
Calculate grouped case fatality
}
\examples{
dados <- data.frame(doenca = c("A", "A"), obitos = c(1, 2),
                    casos = c(20, 30))
letalidade(dados, "obitos", "casos", grupo = "doenca")
}
